---
apiVersion: certmanager.k8s.io/v1alpha1
kind: Certificate
metadata:
  name: unipig-all
  namespace: default
spec:
  acme:
    config:
    - domains:
      - '*.<YOUR_NAME>.plgrnd.be'
      dns01:
        provider: route53
  commonName: ""
  dnsNames:
      - '*.<YOUR_NAME>.plgrnd.be'
  secretName: unipig-tls-certs
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
