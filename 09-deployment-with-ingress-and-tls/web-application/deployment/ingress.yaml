kind: Ingress
apiVersion: extensions/v1beta1
metadata:
  name: web-application
  labels:
    app: web-application
    workshop: workshop
spec:
  tls:
    - hosts:
        - web-application.<YOUR_NAME>.plgrnd.be
  rules:
    - host: web-application.<YOUR_NAME>.plgrnd.be
      http:
        paths:
          - path: /
            backend:
              serviceName: web-application
              servicePort: 80
